name: '{{name.snakeCase()}}'
description: '{{name.titleCase()}} feature module'

environment:
  sdk: ^3.7.1

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  mocktail: ^1.0.4
  bdd_widget_test: ^2.1.4{{#remote}}
  json_serializable: ^6.0.1{{/remote}}

resolution: workspace
